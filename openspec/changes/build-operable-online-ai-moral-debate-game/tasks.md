## 1. 工程骨架与质量门禁

- [x] 1.1 建立 pnpm workspace，创建 `apps/web`、`apps/server`、`packages/contracts`，统一 TypeScript、ESLint、Prettier、Vitest 和环境变量校验。
- [x] 1.2 配置 Vite 开发代理与 Fastify 生产静态托管，使 REST 和 Socket.IO 同源运行。
- [x] 1.3 建立共享 Zod 契约并为 REST 请求、Socket 命令、状态快照和 AI 输出生成 TypeScript 类型与契约测试。

## 2. SQLite 数据与生命周期

- [x] 2.1 使用 Drizzle 定义轻量玩家会话、房间、比赛、局快照、命令幂等记录和 AI 裁决记录 Schema，并生成首个迁移。
- [x] 2.2 配置 SQLite WAL、busy timeout、事务封装和房间级串行命令队列，并用并发测试验证无重复状态转换。
- [x] 2.3 实现关键阶段快照、服务重启恢复、绝对截止时间补推进和比赛结束 24 小时清理，并覆盖恢复与清理集成测试。

## 3. 内置牌库与默认卡面

- [x] 3.1 编写不少于 12 张好人、12 张恶人、24 张词条和 5 个列车长人格的首批中文内置牌库，并完成敏感内容人工审查清单。
- [x] 3.2 制作或引入具备明确授权的原创默认人物卡面，禁止复制参考作品角色、字体、表情包、构图和商标。
- [x] 3.3 实现启动时 Zod 校验，检查唯一 ID、字段、分类、资源路径、人格判断规则和最低发牌数量，并为失败路径编写测试。
- [x] 3.4 实现基于加密安全随机源的双人独立发牌和可注入测试种子，验证每人 3 好人、3 恶人、6 词条且单人手牌无重复。

## 4. 游戏领域规则

- [x] 4.1 用纯 TypeScript 领域模块实现三回合状态机、合法转换表、版本控制和阶段权限，并逐状态编写单元测试。
- [x] 4.2 实现每回合选择两名未使用人物、稳定排序的超时补牌和三回合恰好用完人物手牌，并覆盖非法所有权与重复出牌。
- [x] 4.3 实现词条向双方场上人物附加、跨回合永久继承、词条消耗和主动结束/超时结束。
- [x] 4.4 实现双方各一次的攻击—防守流程、一次性锁定提交、2,000 字符限制和发言超时失利。
- [x] 4.5 实现只沉淀胜方核心论据、三回合最终审判、单局计分、配置局数比赛和同分追加局。

## 5. 玩家会话与实时房间

- [ ] 5.1 实现首次访问 HttpOnly 会话、playerId、昵称校验和绑定房间席位的短期重连令牌，并测试令牌篡改、过期和错房间。（MVP 已实现签名会话恢复，尚缺独立席位令牌及其篡改/过期测试。）
- [x] 5.2 实现六位房间码创建/加入、两人容量、准备、房主开局和第三人拒绝逻辑。
- [x] 5.3 实现局数 1–5、各阶段时限和掉线时限的配置校验，保证核心三回合规则不可更改。
- [x] 5.4 实现 Socket.IO 认证、房间广播、私有手牌投影、commandId 幂等和 expectedVersion 冲突响应。
- [ ] 5.5 实现服务端绝对截止时间调度、连接状态、窗口内重连和掉线超时判负，并用两个 Socket 客户端覆盖完整流程。

## 6. AI 裁决网关

- [x] 6.1 实现仅从服务端环境变量读取的 OpenAI 兼容客户端、超时、最多两次重试、关联 ID 和敏感日志脱敏。
- [ ] 6.2 定义并测试攻防裁决、最终轨道选择和哲学评议的提示构建器及 Zod 输出 Schema，防止原始非法输出直接改变状态。
- [x] 6.3 实现人格权重、人物分类、词条极性、有效辩词和服务器种子驱动的确定性降级裁决，并标记 `fallback=true`。
- [ ] 6.4 将 AI 用例接入状态机，验证成功、超时、网络错误、非法 JSON 和连续失败场景均能推进比赛。

## 7. React 玩家端功能

- [x] 7.1 实现首页昵称输入、创建房间、加入房间、房主规则表单、等待室、准备与分享房间码流程。
- [x] 7.2 建立 Socket 客户端状态层，以服务端快照和版本为准，处理连接中、断线、重连、冲突刷新和不可恢复结束。
- [x] 7.3 实现手牌、场上人物、人物背景、累计词条、私有/公开信息和选择确认交互。
- [x] 7.4 实现词条投放、攻击目标选择、限时辩词编辑、提交锁定、防守阅读和超时反馈。
- [x] 7.5 实现单轮 AI 裁决、最终拉杆结果、哲学评议、局比分、追加局和比赛结束页面。

## 8. 原创荒诞漫画视觉与可访问性

- [x] 8.1 建立原创设计令牌：黑白主色、有限警示红、粗线边框、网点纹理、撕纸形状、字体层级和阴影规则。
- [x] 8.2 为首页、等待室、牌桌、攻防和审判页面实现统一漫画式构图、夸张反馈和不遮挡正文的动效。
- [x] 8.3 完成 1440×900 与 1280×720 桌面布局，较小视口显示横屏/桌面提示。
- [x] 8.4 完成键盘焦点、可访问名称、非颜色状态表达、文本对比度和 `prefers-reduced-motion` 验证。

## 9. 安全与可靠性

- [ ] 9.1 对 REST 与 Socket 输入统一执行 Zod 校验、Origin 校验、会话校验、阶段权限和错误脱敏。
- [ ] 9.2 为房间创建、加入、重连和辩词提交增加进程内限流与负载上限，并测试拒绝行为不改变游戏状态。
- [x] 9.3 增加结构化日志、请求/房间关联 ID、存活与就绪端点，确保日志不包含 AI 密钥、Cookie、令牌或完整辩词。

## 10. 端到端验收与部署

- [ ] 10.1 使用 Playwright 双浏览器测试创建、加入、三回合完整比赛、多局计分和最终哲学评议。
- [ ] 10.2 增加刷新恢复、短暂断线、掉线判负、各阶段超时、AI 降级和服务重启恢复端到端场景。
- [ ] 10.3 在 Chrome、Edge、Firefox 和 Safari 当前及前一个主要版本完成 1280×720 最低视口兼容性验收。
- [x] 10.4 创建非 root 多阶段 Docker 镜像、SQLite 持久卷、健康检查和示例反向代理/TLS 部署说明。
- [ ] 10.5 在不含真实密钥的全新环境执行迁移、启动校验、双设备公网烟测、备份恢复和上一镜像回滚演练。
